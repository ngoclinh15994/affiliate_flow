# Spring Boot application properties for Shopee Browser Agent Backend (MVP)

server.port=8080
spring.application.name=shopee-browser-agent-backend

# Logging level (adjust as needed)
logging.level.org.springframework.web=INFO
logging.level.com.agent=DEBUG

